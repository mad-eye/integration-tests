#! /bin/sh

#Use --production to npm install --production
if [ "$1" = "--production" ]; then
    production="--production"
    echo Using production flag: $production
else
    production=""
fi

basedir=`dirname $0`

npm install -q $production ../azkaban/.madeye-common
npm install -q $production

